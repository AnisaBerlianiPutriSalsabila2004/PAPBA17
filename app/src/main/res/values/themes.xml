<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Fragment_android." parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- Base application theme with ActionBar. -->
    <style name="Base.Theme.Fragment_android.WithActionBar" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here, similar to the theme without ActionBar. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- Themes that can be applied to the application or specific activities. -->
    <style name="Theme.MyCourse" parent="Base.Theme.Fragment_android" />
    <style name="Theme.MyCourse.WithActionBar" parent="Base.Theme.Fragment_android.WithActionBar" />

</resources>
